import { RegxValidatorDirective } from './regx-validator.directive';

describe('RegxValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegxValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
